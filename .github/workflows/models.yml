name: Refresh Provider Models

on:
  workflow_dispatch:
  schedule:
    - cron: "22 3 * * *"

permissions:
  contents: write
  pull-requests: write

jobs:
  refresh-models:
    name: Refresh Provider Models
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Refresh provider models
        run: pnpm run models:refresh -- --summary "$RUNNER_TEMP/pr-body.md"

      - name: Open pull request
        uses: peter-evans/create-pull-request@v7
        with:
          commit-message: "chore: refresh provider model registry"
          title: "chore: refresh provider model registry"
          body-path: ${{ runner.temp }}/pr-body.md
          branch: chore/refresh-provider-models
          delete-branch: true
